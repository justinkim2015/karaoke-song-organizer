<div class="flex flex-col">
  <h1 class='text-xl m-2 p-2'><%= link_to @song.title, @song.lyrics_url %> - <%= @song.artist %></h1>
  
  <div class="box"><%= simple_format(@song.lyrics, class: 'm-2 p-2') %></div>
</div>